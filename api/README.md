# Módulo Backend - API REST (Java + Docker)

Servidor backend encargado de la lógica de negocio y comunicación con PostgreSQL.

## Ejecución Local con Docker (Recomendado)

1. Desde la raíz del repositorio, inicia la base de datos y la API en contenedores:
   ```bash
   docker-compose up --build -d
   ```
